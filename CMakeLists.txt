cmake_minimum_required(VERSION 3.16) #wymagana wersja kompilatora
include($ENV{IDF_PATH}/tools/cmake/project.cmake) #automatyczna konfiguracja procesora, pamięci, flag kompilatora
project(krokomierz) #nazwa aplikacji